// 测试订单支付成功后的回调处理

// 当0.01元测试订单支付成功时，webhook会记录支付时间
// 此外，我们也需要在前端检测订单状态并显示时间
// 但是这需要一个轮询机制或用户手动查询

// 为了在前端显示成功信息，我们可能需要实现一个轮询机制
// 这里我们不实现轮询，但可以添加一个按钮让用户手动查询当前时间

// 在支付成功后，用户可以访问 /api/get-time 来获取当前时间